{
  "scripts": {
    "start": "http-server",
    "build": "mkdir -p build && cp ./site/* ./build",
    "validate": "swagger-cli validate site/pagdumala-api-docs.json"
  },
  "dependencies": {
    "@apidevtools/swagger-cli": "^4.0.4",
    "http-server": "^14.1.0",
    "redoc": "^2.0.0-rc.56"
  }
}